---
name: "005-git-commit"
description: "按规范格式提交代码到本地仓库（仅 commit，不推送）"
---

# 005-git-commit

Use this skill when the user wants a local git commit only.

Rules:
1. Review the diff before staging anything.
2. Stage changes in logical groups rather than using `git add .`.
3. Keep the commit message concise and descriptive.
4. Do not push from this skill.

