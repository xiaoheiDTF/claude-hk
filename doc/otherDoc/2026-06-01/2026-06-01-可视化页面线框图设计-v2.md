# claude-tap-plus 可视化页面线框图设计（基于已有 API）

> 文档日期：2026-06-01
> 严格基于后端已有 API 和数据库表结构设计，不假设任何未实现的接口。

---

## 一、已有 API 清单（严格限定）

| 端点 | 方法 | 功能 | 请求/响应 |
|------|------|------|-----------|
| `/health` | GET | 健康检查 | `{"status":"ok"}` |
| `/api/issue/check` | POST | 检查 Issue 状态 | `CheckIssuesRequest` / `CheckIssuesResponse` |
| `/api/issue/claim` | POST | 领取 Issue | `ClaimIssueRequest` / `ClaimIssueResponse` |
| `/api/issue/release` | POST | 释放 Issue | `ReleaseIssueRequest` / `ReleaseIssueResponse` |
| `/api/issue/release-session` | POST | 释放会话下所有 Issue | `ReleaseSessionRequest` / `ReleaseSessionResponse` |
| `/api/issue/status` | POST | 更新 Issue 状态 | `UpdateStatusRequest` / `UpdateStatusResponse` |
| `/api/session/register` | POST | 注册会话 | `RegisterSessionRequest` / `{"status":"registered"}` |
| `/api/session/close` | POST | 关闭会话 | `CloseSessionRequest` / `{"status":"closed"}` |
| `/api/sessions` | GET | 获取会话列表（支持过滤） | Query: `machine_id`, `project_slug`, `status` / `SessionListResponse` |
| `/api/session/{id}` | GET | 获取单个会话详情 | Path: session_id / `SessionDetail` |
| `/api/proxy/register` | POST | 代理注册 | `{"pid":"","proxy_url":""}` |
| `/api/proxy/unregister` | POST | 代理注销 | `{"pid":""}` |
| `/api/proxy/trace-init` | POST | 转发 trace-init | 请求体原样转发 |

---

## 二、已有数据模型

### 2.1 IssueClaim（issue_claims 表）

```go
type IssueClaim struct {
    ID           int64       // 自增主键
    RepoFullName string      // 仓库全名
    IssueNumber  int         // GitHub issue 编号
    IssueTitle   string      // issue 标题（缓存）
    Status       IssueStatus // idle/claimed/fixing/ready-for-pr/pr-created/testing/reviewing/merged/rejected
    SessionID    string      // 领取者 session_id，idle 时为空
    ClaimedAt    *time.Time  // 领取时间
    UpdatedAt    time.Time   // 最后更新时间
}
```

### 2.2 Session（sessions 表）

```go
type Session struct {
    ID             int64      // 自增主键
    SessionID      string     // UUID
    MachineID      string     // whoami@hostname
    OS             string     // windows/linux/macos
    ProjectSlug    string     // 项目标识
    ProjectCwd     string     // 工作目录
    TranscriptPath string     // 对话记录路径
    LocalTracePath string     // 本地 trace 文件路径
    Model          string     // 使用的模型
    Source         string     // startup/resume
    Status         string     // active/closed
    RegisteredAt   time.Time  // 注册时间
    ClosedAt       *time.Time // 关闭时间
    CloseReason    string     // 关闭原因
}
```

### 2.3 Machine（machines 表）

```go
type Machine struct {
    ID          int64     // 自增主键
    MachineID   string    // whoami@hostname，UNIQUE
    OS          string    // windows/linux/macos
    Hostname    string    // 主机名
    Username    string    // 用户名
    FirstSeenAt time.Time // 首次注册时间
    LastSeenAt  time.Time // 最后活跃时间
}
```

### 2.4 Project（projects 表）

```go
type Project struct {
    ID          int64     // 自增主键
    ProjectSlug string    // 项目标识，UNIQUE
    ProjectCwd  string    // 工作目录
    FirstSeenAt time.Time // 首次出现时间
    LastSeenAt  time.Time // 最后活跃时间
}
```

---

## 三、页面设计（严格基于已有 API）

### 3.1 仪表盘页面 (`/dashboard`)

**可用数据来源**：
- `GET /health` — 服务状态
- `GET /api/sessions?status=active` — 活跃会话数
- `GET /api/sessions` — 总会话数（需前端计数）

**不可用的数据（页面中不展示）**：
- ❌ Issue 总数统计（无 `GET /api/issues` 接口）
- ❌ 机器列表（无 `GET /api/machines` 接口）
- ❌ 项目列表（无 `GET /api/projects` 接口）
- ❌ Token 使用统计（无相关接口）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  📊 Dashboard                                                        │
│  📊  │                                                                      │
│  ──  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  🖥️  │  │ 🟢 活跃   │ │ 📋 总会话 │ │ 🖥️ 机器   │ │ 📁 项目   │              │
│  📋  │  │ Sessions │ │  数量    │ │  数量    │ │  数量    │              │
│      │  │          │ │          │ │          │ │          │              │
│      │  │   12     │ │   45     │ │   --     │ │   --     │              │
│      │  │ (实时)   │ │ (累计)   │ │ (无接口) │ │ (无接口) │              │
│      │  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  ⚠️  限制说明                                                   │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  以下数据需要后端扩展 API 才能展示：                              │ │
│      │  │  • Issue 状态分布（需要 GET /api/issues）                        │ │
│      │  │  • 机器列表（需要 GET /api/machines）                            │ │
│      │  │  • 项目列表（需要 GET /api/projects）                            │ │
│      │  │  • Token 统计（需要 GET /api/session/{id}/tokens）               │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  🔔 最近会话活动（来自 GET /api/sessions）                        │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  [🟢] 14:32  abc123...  project-x   user@host-1   active       │ │
│      │  │  [🟢] 14:28  def456...  project-y   user@host-2   active       │ │
│      │  │  [🔴] 14:15  xyz789...  project-x   user@host-1   closed       │ │
│      │  │  [🟢] 14:10  ghi012...  project-z   user@host-3   active       │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

---

### 3.2 会话列表页面 (`/sessions`)

**可用 API**：
- `GET /api/sessions?machine_id=xxx&project_slug=xxx&status=xxx` — 列表 + 过滤
- `GET /api/session/{id}` — 详情
- `POST /api/session/close` — 手动关闭

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  🖥️ 会话列表                                                         │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  ──  │  │  🔍 搜索 Session ID...                                          │ │
│  📋  │  │  [状态 ▼] [项目 ▼] [机器 ▼]              [🔄 刷新]            │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  Session ID        │ 项目      │ 机器        │ 模型      │ 状态 │ │
│      │  │────────────────────┼───────────┼─────────────┼───────────┼──────│ │
│      │  │ abc123-def4...     │ project-x │ user@host-1 │ sonnet    │ 🟢   │ │
│      │  │ def456-ghi7...     │ project-y │ user@host-2 │ opus      │ 🟢   │ │
│      │  │ xyz789-jkl0...     │ project-x │ user@host-1 │ sonnet    │ 🔴   │ │
│      │  │ ghi012-mno3...     │ project-z │ user@host-3 │ haiku     │ 🟢   │ │
│      │  │ jkl345-pqr6...     │ project-y │ user@host-2 │ sonnet    │ 🔴   │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  [点击行查看详情]                                                      │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

**会话详情抽屉（点击行展开）**：

```
┌──────────────────────────────────────────┐
│  Session 详情                           ×│
├──────────────────────────────────────────┤
│                                          │
│  📊 基本信息（来自 GET /api/session/{id}） │
│  ──────────────────────────────────────  │
│  Session ID: abc123-def456-ghi789        │
│  机器: user@host-1 (windows)             │
│  项目: project-x                         │
│  工作目录: D:\code\project-x             │
│  模型: claude-sonnet-4-20250514          │
│  来源: startup                           │
│  状态: 🟢 active                         │
│  注册时间: 2026-06-01 14:32:15           │
│                                          │
│  📁 Trace 文件                            │
│  ──────────────────────────────────────  │
│  路径: .traces/2026-06-01/trace_1430...  │
│  [📥 下载]（通过 local_trace_path 推断）   │
│                                          │
│  ⚠️  关联 Issue（无法展示）                │
│  ──────────────────────────────────────  │
│  需要后端提供 GET /api/session/{id}/issues │
│  或前端通过 CheckIssues 逐个查询           │
│                                          │
│  [🗑️ 关闭会话]  ← POST /api/session/close │
│                                          │
└──────────────────────────────────────────┘
```

---

### 3.3 Issue 管理页面 (`/issues`)

**可用 API**：
- `POST /api/issue/check` — 查询 Issue 状态（需预先知道 repo + issue_numbers）
- `POST /api/issue/claim` — 领取
- `POST /api/issue/release` — 释放
- `POST /api/issue/status` — 更新状态

**重要限制**：
- ❌ 无 `GET /api/issues` 接口，无法列出所有 Issue
- ❌ 必须预先知道 `repo_full_name` 和 `issue_numbers` 才能查询
- ✅ 页面设计为"按仓库查询"模式，而非全局列表

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  📋 Issue 管理                                                       │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  📋  │  │  仓库: [xiaoheiDTF/claude-hk    ]                               │ │
│  ──  │  │  Issue 编号: [42,45,38,51,44   ]  ← 逗号分隔                    │ │
│      │  │                                          [🔍 查询状态]        │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  查询结果（来自 POST /api/issue/check）                          │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │                                                                  │ │
│      │  │  Issue #42 - Fix auth bug                                       │ │
│      │  │  ├─ 状态: 🟡 idle                                               │ │
│      │  │  ├─ 领取者: --                                                  │ │
│      │  │  └─ [✋ 领取]  ← POST /api/issue/claim                         │ │
│      │  │                                                                  │ │
│      │  │  Issue #45 - Add test coverage                                  │ │
│      │  │  ├─ 状态: 🔵 claimed                                            │ │
│      │  │  ├─ 领取者: session def456...                                   │ │
│      │  │  ├─ 领取时间: 2026-06-01 14:28                                  │ │
│      │  │  ├─ [✏️ 更新状态 ▼]  ← POST /api/issue/status                   │ │
│      │  │  └─ [🗑️ 释放]  ← POST /api/issue/release                       │ │
│      │  │                                                                  │ │
│      │  │  Issue #38 - Refactor API                                       │ │
│      │  │  ├─ 状态: 🟠 fixing                                             │ │
│      │  │  ├─ 领取者: session abc123...                                   │ │
│      │  │  └─ [✏️ 更新状态 ▼] [🗑️ 释放]                                   │ │
│      │  │                                                                  │ │
│      │  │  Issue #51 - Update docs                                        │ │
│      │  │  ├─ 状态: 🟣 ready-for-pr                                       │ │
│      │  │  ├─ 领取者: session ghi789...                                   │ │
│      │  │  └─ [✏️ 更新状态 ▼] [🗑️ 释放]                                   │ │
│      │  │                                                                  │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ⚠️  提示：输入 Issue 编号后点击查询，不支持列出所有 Issue            │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

**状态更新下拉菜单**：
```
[✏️ 更新状态 ▼]
    ├─ idle
    ├─ claimed
    ├─ fixing
    ├─ ready-for-pr
    ├─ pr-created
    ├─ testing
    ├─ reviewing
    ├─ merged
    └─ rejected
```

---

### 3.4 机器与项目信息页面 (`/info`)

**可用数据来源**：
- ❌ 无 `GET /api/machines` 接口
- ❌ 无 `GET /api/projects` 接口
- ✅ 只能从 `GET /api/sessions` 的响应中**提取** machine_id 和 project_slug

**设计思路**：从会话列表反推机器和项目信息

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  🖥️ 机器与项目（从会话数据提取）                                      │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────┐ ┌────────────────────────────────────┐ │
│  ──  │  │  🖥️ 机器（去重）        │ │  📁 项目（去重）                    │ │
│  📋  │  │  ─────────────────────  │ │  ────────────────────────────────── │ │
│      │  │                         │ │                                     │ │
│      │  │  user@host-1 (windows)  │ │  project-x                          │ │
│      │  │  ├─ 活跃会话: 3         │ │  ├─ 会话数: 25                      │ │
│      │  │  ├─ 总会话: 12          │ │  ├─ 活跃: 2                         │ │
│      │  │  └─ 最后活跃: 14:32     │ │  └─ 最后活跃: 14:32                 │ │
│      │  │                         │ │                                     │ │
│      │  │  user@host-2 (linux)    │ │  project-y                          │ │
│      │  │  ├─ 活跃会话: 2         │ │  ├─ 会话数: 12                      │ │
│      │  │  ├─ 总会话: 8           │ │  ├─ 活跃: 2                         │ │
│      │  │  └─ 最后活跃: 14:28     │ │  └─ 最后活跃: 14:28                 │ │
│      │  │                         │ │                                     │ │
│      │  │  user@host-3 (macos)    │ │  project-z                          │ │
│      │  │  ├─ 活跃会话: 1         │ │  ├─ 会话数: 8                       │ │
│      │  │  ├─ 总会话: 3           │ │  ├─ 活跃: 1                         │ │
│      │  │  └─ 最后活跃: 14:10     │ │  └─ 最后活跃: 14:10                 │ │
│      │  │                         │ │                                     │ │
│      │  └────────────────────────┘ └────────────────────────────────────┘ │
│      │                                                                      │
│      │  ⚠️  数据来源：从 GET /api/sessions 响应中提取 machine_id 和        │
│      │      project_slug 后前端去重统计，非直接查询 machines/projects 表    │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

---

### 3.5 系统状态页面 (`/status`)

**可用 API**：
- `GET /health` — 服务健康状态
- `GET /api/sessions?status=active` — 活跃会话

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  📡 系统状态                                                         │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  📋  │  │  后端服务状态                                                   │ │
│  ──  │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  🟢 运行中  │  最后检查: 2026-06-01 14:35:22                    │ │
│      │  │       ↑ GET /health 返回 {"status":"ok"}                      │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  活跃会话（来自 GET /api/sessions?status=active）               │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  Session ID        │ 项目      │ 机器        │ 注册时间        │ │
│      │  │────────────────────┼───────────┼─────────────┼────────────────│ │
│      │  │ abc123-def4...     │ project-x │ user@host-1 │ 14:32:15       │ │
│      │  │ def456-ghi7...     │ project-y │ user@host-2 │ 14:28:40       │ │
│      │  │ ghi012-mno3...     │ project-z │ user@host-3 │ 14:10:22       │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  ⚠️  无法展示的数据（需要后端扩展 API）                          │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  • 代理注册列表（无 GET /api/proxies）                           │ │
│      │  │  • Issue 统计（无 GET /api/issues/stats）                        │ │
│      │  │  • 系统日志（无 GET /api/logs）                                  │ │
│      │  │  • 配置信息（无 GET /api/config）                                │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

---

## 四、全局组件设计

### 4.1 顶部导航栏

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus    │  仪表盘  │  会话  │  Issue  │  信息  │    [⚙️]     │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 导航项 | 对应页面 | 可用 API |
|--------|----------|----------|
| 仪表盘 | `/dashboard` | `GET /health`, `GET /api/sessions` |
| 会话 | `/sessions` | `GET /api/sessions`, `GET /api/session/{id}`, `POST /api/session/close` |
| Issue | `/issues` | `POST /api/issue/check/claim/release/status` |
| 信息 | `/info` | `GET /api/sessions`（提取 machine/project） |

### 4.2 状态标签颜色

| 状态 | 颜色 | 适用场景 |
|------|------|----------|
| active | 🟢 绿色 | Session 状态 |
| closed | 🔴 红色 | Session 状态 |
| idle | 🟡 黄色 | Issue 状态 |
| claimed | 🔵 蓝色 | Issue 状态 |
| fixing | 🟠 橙色 | Issue 状态 |
| ready-for-pr | 🟣 紫色 | Issue 状态 |
| pr-created | 🔷 靛蓝 | Issue 状态 |
| testing | 🟤 棕色 | Issue 状态 |
| reviewing | ⚪ 灰色 | Issue 状态 |
| merged | ✅ 深绿 | Issue 终态 |
| rejected | ❌ 深红 | Issue 终态 |

---

## 五、前端数据流设计

### 5.1 会话列表页数据流

```
页面加载
    │
    ▼
GET /api/sessions
    │
    ▼
┌─────────────────────────────────────────┐
│ 响应: SessionListResponse               │
│ {                                       │
│   "sessions": [                         │
│     { "session_id": "abc...",           │
│       "machine_id": "user@host-1",      │
│       "project_slug": "project-x",      │
│       "status": "active",               │
│       "registered_at": "..." },         │
│     ...                                 │
│   ]                                     │
│ }                                       │
└─────────────────────────────────────────┘
    │
    ▼
渲染表格
    │
    ▼
用户点击行
    │
    ▼
GET /api/session/{id}
    │
    ▼
渲染详情抽屉（SessionDetail）
```

### 5.2 Issue 管理页数据流

```
用户输入仓库名 + Issue 编号
    │
    ▼
POST /api/issue/check
Body: { "repo_full_name": "owner/repo", "issue_numbers": [42,45] }
    │
    ▼
┌─────────────────────────────────────────┐
│ 响应: CheckIssuesResponse               │
│ {                                       │
│   "issues": [                           │
│     { "number": 42, "status": "idle",    │
│       "session_id": null,               │
│       "claimed_at": null },             │
│     { "number": 45, "status": "claimed", │
│       "session_id": "def456...",        │
│       "claimed_at": "2026-06-01T14:28" }│
│   ]                                     │
│ }                                       │
└─────────────────────────────────────────┘
    │
    ▼
渲染 Issue 卡片列表
    │
    ▼
用户操作（领取/释放/更新状态）
    │
    ▼
POST /api/issue/claim 或 /release 或 /status
    │
    ▼
操作成功后重新查询刷新
```

---

## 六、API 能力缺口清单

以下功能在页面设计中被标记为"不可用"，需要后端扩展 API：

| 缺失 API | 影响页面 | 功能描述 |
|----------|----------|----------|
| `GET /api/issues` | Issue 看板 | 无法列出所有 Issue，只能逐个仓库查询 |
| `GET /api/issues?repo=xxx` | Issue 看板 | 无法按仓库列出所有 Issue |
| `GET /api/machines` | 机器管理 | 无法直接查询机器表 |
| `GET /api/projects` | 项目管理 | 无法直接查询项目表 |
| `GET /api/session/{id}/issues` | 会话详情 | 无法查看会话关联的 Issue |
| `GET /api/session/{id}/tokens` | 会话详情 | 无法显示 Token 统计 |
| `GET /api/session/{id}/traces` | Trace 查看 | 无法列出 Trace 文件 |
| `GET /api/logs` | 系统日志 | 无法查询后端日志 |
| `GET /api/config` | 设置 | 无法查看系统配置 |
| `PUT /api/config` | 设置 | 无法修改系统配置 |
| `GET /api/proxies` | 系统状态 | 无法查看代理注册列表 |
| `GET /api/status` | 仪表盘 | 无法获取聚合统计指标 |

---

## 七、技术栈建议

| 层级 | 技术 |
|------|------|
| 前端框架 | React 18 + TypeScript |
| UI 组件库 | Ant Design 5.x |
| 状态管理 | React Query (TanStack Query) |
| HTTP 客户端 | Axios / Fetch |
| 路由 | React Router v6 |
| 构建工具 | Vite |
| 样式 | CSS Modules / Tailwind CSS |

---

*文档结束*
