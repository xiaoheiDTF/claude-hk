# claude-tap-plus 可视化页面线框图设计（基于已有 API）

> 文档日期：2026-06-01
> 严格基于后端已有 API 和数据库表结构设计，不假设任何未实现的接口。

---

## 一、已有 API 清单（严格限定）

| 端点 | 方法 | 功能 | 请求/响应 |
|------|------|------|-----------|
| `/health` | GET | 健康检查 | `{"status":"ok"}` |
| `/api/issue/check` | POST | 检查 Issue 状态 | `CheckIssuesRequest` / `CheckIssuesResponse` |
| `/api/issue/claim` | POST | 领取 Issue | `ClaimIssueRequest` / `ClaimIssueResponse` |
| `/api/issue/release` | POST | 释放 Issue | `ReleaseIssueRequest` / `ReleaseIssueResponse` |
| `/api/issue/release-session` | POST | 释放会话下所有 Issue | `ReleaseSessionRequest` / `ReleaseSessionResponse` |
| `/api/issue/status` | POST | 更新 Issue 状态 | `UpdateStatusRequest` / `UpdateStatusResponse` |
| `/api/issues` | GET | Issue 列表（过滤+分页） | Query: `repo`, `status`, `session_id`, `page`, `page_size` / `IssuesListResponse` |
| `/api/session/register` | POST | 注册会话 | `RegisterSessionRequest` / `{"status":"registered"}` |
| `/api/session/close` | POST | 关闭会话 | `CloseSessionRequest` / `{"status":"closed"}` |
| `/api/sessions` | GET | 获取会话列表（支持过滤） | Query: `machine_id`, `project_slug`, `status` / `SessionListResponse` |
| `/api/session/{id}` | GET | 获取单个会话详情 | Path: session_id / `SessionDetail` |
| `/api/session/{id}/issues` | GET | 获取会话关联 Issue | Path: session_id / `SessionIssuesResponse` |
| `/api/session/{id}/tokens` | GET | 获取会话 Token 统计 | Path: session_id / `SessionTokensResponse` |
| `/api/session/{id}/traces` | GET | 获取会话 Trace 文件 | Path: session_id / `SessionTracesResponse` |
| `/api/machines` | GET | 获取机器列表 | Query: `os`, `hostname` / `MachinesListResponse` |
| `/api/projects` | GET | 获取项目列表 | — / `ProjectsListResponse` |
| `/api/proxy/register` | POST | 代理注册 | `{"pid":"","proxy_url":""}` |
| `/api/proxy/unregister` | POST | 代理注销 | `{"pid":""}` |
| `/api/proxy/trace-init` | POST | 转发 trace-init | 请求体原样转发 |
| `/api/proxies` | GET | 列出代理注册列表 | Query: `status`, `project` / `ProxiesResponse` |
| `/api/status` | GET | 系统聚合状态 | `StatusResponse`（含 `SystemStats`） |
| `/api/logs` | GET | 查询后端日志 | Query: `level`, `date`, `limit` / `LogsResponse` |
| `/api/config` | GET | 获取系统配置 | — / `ConfigResponse` |
| `/api/config` | PUT | 更新系统配置 | `map[string]interface{}` / `ConfigResponse` |

---

## 二、已有数据模型

### 2.1 IssueClaim（issue_claims 表）

```go
type IssueClaim struct {
    ID           int64       // 自增主键
    RepoFullName string      // 仓库全名
    IssueNumber  int         // GitHub issue 编号
    IssueTitle   string      // issue 标题（缓存）
    Status       IssueStatus // idle/claimed/fixing/ready-for-pr/pr-created/testing/reviewing/merged/rejected
    SessionID    string      // 领取者 session_id，idle 时为空
    ClaimedAt    *time.Time  // 领取时间
    UpdatedAt    time.Time   // 最后更新时间
}
```

### 2.2 Session（sessions 表）

```go
type Session struct {
    ID             int64      // 自增主键
    SessionID      string     // UUID
    MachineID      string     // whoami@hostname
    OS             string     // windows/linux/macos
    ProjectSlug    string     // 项目标识
    ProjectCwd     string     // 工作目录
    TranscriptPath string     // 对话记录路径
    LocalTracePath string     // 本地 trace 文件路径
    Model          string     // 使用的模型
    Source         string     // startup/resume
    Status         string     // active/closed
    RegisteredAt   time.Time  // 注册时间
    ClosedAt       *time.Time // 关闭时间
    CloseReason    string     // 关闭原因
}
```

### 2.3 Machine（machines 表）

```go
type Machine struct {
    ID          int64     // 自增主键
    MachineID   string    // whoami@hostname，UNIQUE
    OS          string    // windows/linux/macos
    Hostname    string    // 主机名
    Username    string    // 用户名
    FirstSeenAt time.Time // 首次注册时间
    LastSeenAt  time.Time // 最后活跃时间
}
```

### 2.4 Project（projects 表）

```go
type Project struct {
    ID          int64     // 自增主键
    ProjectSlug string    // 项目标识，UNIQUE
    ProjectCwd  string    // 工作目录
    FirstSeenAt time.Time // 首次出现时间
    LastSeenAt  time.Time // 最后活跃时间
}
```

### 2.5 Proxy（proxies 表）

```go
type Proxy struct {
    ProxyID      string     // 代理唯一标识（PID），PRIMARY KEY
    ProjectSlug  string     // 关联项目
    Status       string     // 代理状态：active / offline
    RegisteredAt time.Time  // 注册时间
    LastPingAt   *time.Time // 最后心跳时间（可为空）
}
```

---

## 三、页面设计（严格基于已有 API）

### 3.1 仪表盘页面 (`/dashboard`)

**可用数据来源**：
- `GET /health` — 服务状态
- `GET /api/status` — 聚合统计指标（活跃会话、活跃代理、待处理 Issue、机器总数、项目总数）
- `GET /api/sessions?status=active` — 活跃会话数
- `GET /api/issues` — Issue 列表（支持分页，可获取总数）
- `GET /api/machines` — 机器列表
- `GET /api/projects` — 项目列表

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  📊 Dashboard                                                        │
│  📊  │                                                                      │
│  ──  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  🖥️  │  │ 🟢 活跃   │ │ ⏳ 待处理 │ │ 🟢 活跃   │ │ 🖥️ 机器   │ │ 📁 项目   │ │
│  📋  │  │ Sessions │ │ Issue   │ │ 代理     │ │  总数    │ │  总数    │ │
│      │  │          │ │          │ │          │ │          │ │          │ │
│      │  │   12     │ │    8     │ │    3     │ │    5     │ │    7     │ │
│      │  │ (实时)   │ │ (实时)   │ │ (实时)   │ │ (实时)   │ │ (实时)   │ │
│      │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│      │         ↑ 数据来源：GET /api/status → stats 字段                     │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  🔔 最近会话活动（来自 GET /api/sessions）                        │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  [🟢] 14:32  abc123...  project-x   user@host-1   active       │ │
│      │  │  [🟢] 14:28  def456...  project-y   user@host-2   active       │ │
│      │  │  [🔴] 14:15  xyz789...  project-x   user@host-1   closed       │ │
│      │  │  [🟢] 14:10  ghi012...  project-z   user@host-3   active       │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  📊 Issue 状态分布（来自 GET /api/issues?status=xxx 计数）        │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  idle: 8  │  claimed: 3  │  fixing: 2  │  merged: 12  │  ...   │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

---

### 3.2 会话列表页面 (`/sessions`)

**可用 API**：
- `GET /api/sessions?machine_id=xxx&project_slug=xxx&status=xxx` — 列表 + 过滤
- `GET /api/session/{id}` — 详情
- `GET /api/session/{id}/issues` — 会话关联 Issue
- `GET /api/session/{id}/tokens` — Token 统计
- `GET /api/session/{id}/traces` — Trace 文件
- `POST /api/session/close` — 手动关闭

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  🖥️ 会话列表                                                         │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  ──  │  │  🔍 搜索 Session ID...                                          │ │
│  📋  │  │  [状态 ▼] [项目 ▼] [机器 ▼]              [🔄 刷新]            │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  Session ID        │ 项目      │ 机器        │ 模型      │ 状态 │ │
│      │  │────────────────────┼───────────┼─────────────┼───────────┼──────│ │
│      │  │ abc123-def4...     │ project-x │ user@host-1 │ sonnet    │ 🟢   │ │
│      │  │ def456-ghi7...     │ project-y │ user@host-2 │ opus      │ 🟢   │ │
│      │  │ xyz789-jkl0...     │ project-x │ user@host-1 │ sonnet    │ 🔴   │ │
│      │  │ ghi012-mno3...     │ project-z │ user@host-3 │ haiku     │ 🟢   │ │
│      │  │ jkl345-pqr6...     │ project-y │ user@host-2 │ sonnet    │ 🔴   │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  [点击行查看详情]                                                      │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

**会话详情抽屉（点击行展开）**：

```
┌──────────────────────────────────────────┐
│  Session 详情                           ×│
├──────────────────────────────────────────┤
│                                          │
│  📊 基本信息（来自 GET /api/session/{id}） │
│  ──────────────────────────────────────  │
│  Session ID: abc123-def456-ghi789        │
│  机器: user@host-1 (windows)             │
│  项目: project-x                         │
│  工作目录: D:\code\project-x             │
│  模型: claude-sonnet-4-20250514          │
│  来源: startup                           │
│  状态: 🟢 active                         │
│  注册时间: 2026-06-01 14:32:15           │
│                                          │
│  📋 关联 Issue（来自 GET /api/session/{id}/issues）│
│  ──────────────────────────────────────  │
│  #42 Fix auth bug    🟡 idle             │
│  #38 Refactor API    🟠 fixing           │
│                                          │
│  📊 Token 统计（来自 GET /api/session/{id}/tokens）│
│  ──────────────────────────────────────  │
│  API 调用: 15                            │
│  输入 Token: 12,340                      │
│  输出 Token: 8,560                       │
│  缓存读取: 5,200                         │
│  缓存创建: 1,800                         │
│  总 Token: 20,900                        │
│                                          │
│  📁 Trace 文件（来自 GET /api/session/{id}/traces）│
│  ──────────────────────────────────────  │
│  trace_143052.jsonl  │ 1.2MB │ 342 行   │
│                                          │
│  [🗑️ 关闭会话]  ← POST /api/session/close │
│                                          │
└──────────────────────────────────────────┘
```

---

### 3.3 Issue 管理页面 (`/issues`)

**可用 API**：
- `GET /api/issues` — 列出所有 Issue（支持 `?repo=`, `?status=`, `?session_id=`, `?page=`, `?page_size=`）
- `POST /api/issue/check` — 查询指定 Issue 状态
- `POST /api/issue/claim` — 领取
- `POST /api/issue/release` — 释放
- `POST /api/issue/status` — 更新状态

**设计说明**：
- ✅ 支持全局 Issue 列表浏览（GET /api/issues）
- ✅ 支持按仓库、状态、会话过滤
- ✅ 支持分页浏览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  📋 Issue 管理                                                       │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  📋  │  │  🔍 搜索 Issue...                                               │ │
│  ──  │  │  [状态 ▼] [仓库 ▼] [会话 ▼]                [🔄 刷新]          │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  Issue 列表（来自 GET /api/issues）                              │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  #  │ 仓库              │ 标题        │ 状态    │ 持有者      │ │
│      │  │─────┼───────────────────┼─────────────┼─────────┼─────────────│ │
│      │  │  42 │ xiaoheiDTF/claude │ Fix auth    │ 🟡 idle │ --          │ │
│      │  │  45 │ xiaoheiDTF/claude │ Add test    │ 🔵claim │ def456...   │ │
│      │  │  38 │ xiaoheiDTF/claude │ Refactor    │ 🟠 fix  │ abc123...   │ │
│      │  │  51 │ xiaoheiDTF/claude │ Update docs │ 🟣 r-pr │ ghi789...   │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  [点击行查看详情 / 操作: ✋ 领取 | ✏️ 更新状态 | 🗑️ 释放]            │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  分页: [← 上一页] 第 1/3 页（共 42 条） [下一页 →]              │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

**状态更新下拉菜单**：
```
[✏️ 更新状态 ▼]
    ├─ idle
    ├─ claimed
    ├─ fixing
    ├─ ready-for-pr
    ├─ pr-created
    ├─ testing
    ├─ reviewing
    ├─ merged
    └─ rejected
```

---

### 3.4 机器与项目信息页面 (`/info`)

**可用数据来源**：
- `GET /api/machines` — 直接查询机器列表（支持 `?os=`、`?hostname=` 过滤）
- `GET /api/projects` — 直接查询项目列表

**设计思路**：通过独立 API 直接获取机器和项目信息

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  🖥️ 机器与项目                                                      │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  ──  │  │  🖥️ 机器列表（来自 GET /api/machines）                          │ │
│  📋  │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  机器 ID         │ OS      │ 用户名  │ 首次注册   │ 最后活跃    │ │
│      │  │─────────────────┼─────────┼─────────┼────────────┼────────────│ │
│      │  │  user@host-1    │ windows │ user    │ 06-01 10:00│ 14:32      │ │
│      │  │  user@host-2    │ linux   │ user    │ 06-01 09:00│ 14:28      │ │
│      │  │  user@host-3    │ macos   │ user    │ 06-01 08:00│ 14:10      │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  📁 项目列表（来自 GET /api/projects）                          │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  项目标识     │ 工作目录              │ 首次注册   │ 最后活跃    │ │
│      │  │──────────────┼───────────────────────┼────────────┼────────────│ │
│      │  │  project-x   │ D:\code\project-x     │ 06-01 10:00│ 14:32      │ │
│      │  │  project-y   │ /home/user/project-y  │ 06-01 09:00│ 14:28      │ │
│      │  │  project-z   │ /Users/user/project-z │ 06-01 08:00│ 14:10      │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  数据来源：GET /api/machines + GET /api/projects（直接查询）         │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

---

### 3.5 系统状态页面 (`/status`)

**可用 API**：
- `GET /health` — 服务健康状态
- `GET /api/status` — 系统聚合状态（版本、运行时间、统计摘要）
- `GET /api/proxies` — 代理注册列表（支持 `?status=`、`?project=` 过滤）
- `GET /api/sessions?status=active` — 活跃会话
- `GET /api/logs` — 后端日志查询（支持 `?level=`、`?date=`、`?limit=`）
- `GET /api/config` — 系统配置查看

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus                                    [🔔] [⚙️] [👤]      │
├──────┬──────────────────────────────────────────────────────────────────────┤
│      │  📡 系统状态                                                         │
│  📊  │                                                                      │
│  🖥️  │  ┌────────────────────────────────────────────────────────────────┐ │
│  📋  │  │  系统聚合状态（来自 GET /api/status）                            │ │
│  ──  │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  🟢 运行中  │  版本: v1.0.0  │  运行时间: 2h 35m               │ │
│      │  │  最后检查: 2026-06-01 14:35:22                                 │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  活跃会话: 3  │  活跃代理: 2  │  待处理 Issue: 5               │ │
│      │  │  机器总数: 4  │  项目总数: 6                                    │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  代理注册列表（来自 GET /api/proxies）                           │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  代理 ID       │ 项目        │ 状态    │ 注册时间  │ 最后心跳   │ │
│      │  │────────────────┼─────────────┼─────────┼──────────┼───────────│ │
│      │  │ 28364          │ project-x   │ 🟢 活跃 │ 14:30    │ 14:35     │ │
│      │  │ 15287          │ project-y   │ 🟢 活跃 │ 14:20    │ 14:34     │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  活跃会话（来自 GET /api/sessions?status=active）               │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  Session ID        │ 项目      │ 机器        │ 注册时间        │ │
│      │  │────────────────────┼───────────┼─────────────┼────────────────│ │
│      │  │ abc123-def4...     │ project-x │ user@host-1 │ 14:32:15       │ │
│      │  │ def456-ghi7...     │ project-y │ user@host-2 │ 14:28:40       │ │
│      │  │ ghi012-mno3...     │ project-z │ user@host-3 │ 14:10:22       │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  📋 最近日志（来自 GET /api/logs?limit=10）                      │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  时间         │ 级别  │ 来源       │ 内容                      │ │
│      │  │──────────────┼───────┼────────────┼───────────────────────────│ │
│      │  │  14:35:22     │ INFO  │ api.status │ GET /api/status           │ │
│      │  │  14:35:20     │ DEBUG │ api.issue  │ POST /api/issue/check     │ │
│      │  │  14:35:15     │ WARN  │ svc.token  │ parse trace failed: ...   │ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
│      │  ┌────────────────────────────────────────────────────────────────┐ │
│      │  │  ⚙️ 系统配置（来自 GET /api/config）                            │ │
│      │  │  ────────────────────────────────────────────────────────────  │ │
│      │  │  log_level: info  │ session_timeout: 3600  │ max_concurrent: 10│ │
│      │  └────────────────────────────────────────────────────────────────┘ │
│      │                                                                      │
└──────┴──────────────────────────────────────────────────────────────────────┘
```

---

## 四、全局组件设计

### 4.1 顶部导航栏

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🧠 Claude Tap Plus    │  仪表盘  │  会话  │  Issue  │  信息  │    [⚙️]     │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 导航项 | 对应页面 | 可用 API |
|--------|----------|----------|
| 仪表盘 | `/dashboard` | `GET /health`, `GET /api/sessions`, `GET /api/status`, `GET /api/issues` |
| 会话 | `/sessions` | `GET /api/sessions`, `GET /api/session/{id}`, `GET /api/session/{id}/issues`, `GET /api/session/{id}/tokens`, `GET /api/session/{id}/traces`, `POST /api/session/close` |
| Issue | `/issues` | `GET /api/issues`, `POST /api/issue/check/claim/release/status` |
| 信息 | `/info` | `GET /api/machines`, `GET /api/projects` |

### 4.2 状态标签颜色

| 状态 | 颜色 | 适用场景 |
|------|------|----------|
| active | 🟢 绿色 | Session 状态 |
| closed | 🔴 红色 | Session 状态 |
| idle | 🟡 黄色 | Issue 状态 |
| claimed | 🔵 蓝色 | Issue 状态 |
| fixing | 🟠 橙色 | Issue 状态 |
| ready-for-pr | 🟣 紫色 | Issue 状态 |
| pr-created | 🔷 靛蓝 | Issue 状态 |
| testing | 🟤 棕色 | Issue 状态 |
| reviewing | ⚪ 灰色 | Issue 状态 |
| merged | ✅ 深绿 | Issue 终态 |
| rejected | ❌ 深红 | Issue 终态 |

---

## 五、前端数据流设计

### 5.1 会话列表页数据流

```
页面加载
    │
    ▼
GET /api/sessions
    │
    ▼
┌─────────────────────────────────────────┐
│ 响应: SessionListResponse               │
│ {                                       │
│   "sessions": [                         │
│     { "session_id": "abc...",           │
│       "machine_id": "user@host-1",      │
│       "project_slug": "project-x",      │
│       "status": "active",               │
│       "registered_at": "..." },         │
│     ...                                 │
│   ]                                     │
│ }                                       │
└─────────────────────────────────────────┘
    │
    ▼
渲染表格
    │
    ▼
用户点击行
    │
    ▼
GET /api/session/{id}
    │
    ▼
渲染详情抽屉（SessionDetail）
```

### 5.2 Issue 管理页数据流

```
页面加载
    │
    ▼
GET /api/issues?repo=xxx&status=xxx&page=1&page_size=20
    │
    ▼
┌─────────────────────────────────────────┐
│ 响应: IssuesListResponse                │
│ {                                       │
│   "issues": [                           │
│     { "issue_number": 42,               │
│       "repo_full_name": "owner/repo",   │
│       "status": "idle",                 │
│       "session_id": null },             │
│     ...                                 │
│   ],                                    │
│   "total": 42,                          │
│   "page": 1,                            │
│   "page_size": 20,                      │
│   "total_pages": 3                      │
│ }                                       │
└─────────────────────────────────────────┘
    │
    ▼
渲染 Issue 列表 + 分页
    │
    ▼
用户操作（领取/释放/更新状态）
    │
    ▼
POST /api/issue/claim 或 /release 或 /status
    │
    ▼
操作成功后重新查询刷新
```

---

## 六、API 能力缺口清单

以下功能仍需后端扩展：

| 缺失 API | 影响页面 | 功能描述 |
|----------|----------|----------|
| `GET /api/issue/{repo}/{number}` | Issue 详情 | 无法查看单个 Issue 的完整历史信息 |
| `GET /api/issues/stats` | Issue 统计 | 无法按仓库或全局统计 Issue 状态分布 |
| `GET /api/machine/{id}` | 机器详情 | 无法查看单台机器的关联会话和统计 |
| `GET /api/machine/{id}/stats` | 机器统计 | 无法查看机器的会话数、Issue 处理数等统计 |
| `GET /api/project/{slug}` | 项目详情 | 无法查看项目的关联会话和 Issue 统计 |
| `POST /api/session/{id}/heartbeat` | 心跳检测 | 无法检测异常断连的会话 |

### 已实现的 API（之前在缺口清单中）

| API | 实现版本 | 说明 |
|-----|---------|------|
| `GET /api/issues` | ✅ 已实现 | 支持按仓库、状态、会话过滤 + 分页 |
| `GET /api/machines` | ✅ 已实现 | 支持按 OS、hostname 过滤 |
| `GET /api/projects` | ✅ 已实现 | 按 last_seen_at 倒序 |
| `GET /api/session/{id}/issues` | ✅ 已实现 | 返回会话关联的所有 Issue |
| `GET /api/session/{id}/tokens` | ✅ 已实现 | 解析 trace 文件汇总 token |
| `GET /api/session/{id}/traces` | ✅ 已实现 | 返回 trace 文件元数据 |
| `GET /api/logs` | ✅ 已实现 | 支持级别、日期过滤 |
| `GET /api/config` | ✅ 已实现 | 返回所有配置项 |
| `PUT /api/config` | ✅ 已实现 | 支持部分更新配置 |

---

## 七、技术栈建议

| 层级 | 技术 |
|------|------|
| 前端框架 | React 18 + TypeScript |
| UI 组件库 | Ant Design 5.x |
| 状态管理 | React Query (TanStack Query) |
| HTTP 客户端 | Axios / Fetch |
| 路由 | React Router v6 |
| 构建工具 | Vite |
| 样式 | CSS Modules / Tailwind CSS |

---

*文档结束*
